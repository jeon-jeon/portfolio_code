const TitleText = ({ title }) => {
  return (
    <h1
      style={{
        width: "100%",
        fontSize: "72px",
        color: "#433e49",
      }}
    >
      {title}
    </h1>
  );
};

export default TitleText;
